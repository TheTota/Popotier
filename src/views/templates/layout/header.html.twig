
<nav id="menu" class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="{{ path('home')}}">Popotier</a>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
                <a class="nav-link" href="/home">Accueil <span class="sr-only">(current)</span></a>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <div class="user-actions">
                {% if session.alias %}
                    {% if session.role == 'admin' %}
                        <a class="btn btn-light btn-sm" href="{{ path('admin_dashboard') }}">Administration</a>
                    {% else %}
                        <a class="btn btn-light btn-sm" href="{{ path('user_recipe_list') }}">Profil</a>
                    {% endif %}
                    <a class="btn btn-danger btn-sm" href="{{ path('logout') }}">Deconnexion</a>
                {% else %}
                    <a class="btn btn-outline-primary" href="{{ path('add_user') }}">Inscription</a>
                    <a class="btn btn-outline-primary" href="{{ path('login') }}">Connexion</a>
                {% endif %}
            </div>
        </form>
    </div>
</nav>